<h1>Fieldsets</h1>

<p>
  Mirrors the upstream <a href="https://govuk-form-builder.netlify.app/building-blocks/fieldsets/">Fieldsets</a>
  page. Each example below is a rendered form loaded in its own turbo frame.
</p>

<%= guide_example("fieldsets", "address", "A fieldset for capturing an address") %>

<p><%= link_to("Back to guide", root_path) %></p>
