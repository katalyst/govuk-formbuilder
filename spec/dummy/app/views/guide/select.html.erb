<h1>Select</h1>

<p>
  Mirrors the upstream <a href="https://govuk-form-builder.netlify.app/form-elements/select/">Select</a>
  page. Each example below is a rendered form loaded in its own turbo frame.
</p>

<%= guide_example("select", "with_label_and_hint", "Select field with a label and hint") %>
<%= guide_example("select", "grouped_options", "Taking greater control over the options") %>

<p><%= link_to("Back to guide", root_path) %></p>
