<h1>Password input</h1>

<p>
  Mirrors the upstream <a href="https://govuk-form-builder.netlify.app/form-elements/password-input/">Password input</a>
  page. Each example below is a rendered form loaded in its own turbo frame.
</p>

<%= guide_example("password_input", "default", "A default password input") %>
<%= guide_example("password_input", "captioned", "A password input with a custom label, caption and hint") %>
<%= guide_example("password_input", "custom_text", "Password input with custom text") %>

<p><%= link_to("Back to guide", root_path) %></p>
