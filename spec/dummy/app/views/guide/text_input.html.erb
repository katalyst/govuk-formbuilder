<h1>Text input</h1>

<p>
  Mirrors the upstream <a href="https://govuk-form-builder.netlify.app/form-elements/text-input/">Text input</a>
  page. Each example below is a rendered form loaded in its own turbo frame.
</p>

<%= guide_example("text_input", "with_label_and_hint",
      "Generating a text input with a custom label and hint") %>

<%= guide_example("text_input", "numeric_attributes",
      "Numeric text inputs") %>

<%= guide_example("text_input", "prefix_and_suffix",
      "Inputs with prefixes and suffixes") %>

<%= guide_example("text_input", "custom_width",
      "Generating text inputs with custom widths") %>

<%= guide_example("text_input", "extra_letter_spacing",
      "Inputs with extra spacing between letters") %>

<p><%= link_to("Back to guide", root_path) %></p>
