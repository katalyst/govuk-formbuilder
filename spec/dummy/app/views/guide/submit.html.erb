<h1>Submit button</h1>

<p>
  Mirrors the upstream <a href="https://govuk-form-builder.netlify.app/form-elements/submit/">Submit button</a>
  page. Each example below is a rendered form loaded in its own turbo frame. On
  this page the example button is itself the form's submit (no Succeed/Fail pair).
</p>

<%= guide_example("submit", "default", "Generating a submit button using the default parameters") %>
<%= guide_example("submit", "secondary", "Generating a secondary submit button") %>
<%= guide_example("submit", "warning", "Generating a warning submit button") %>
<%= guide_example("submit", "inverse", "Generating submit button with inverted colours") %>
<%= guide_example("submit", "custom_class", "Generating a submit button with a custom class") %>
<%= guide_example("submit", "no_double_click", "Generating a submit button without double click prevention") %>
<%= guide_example("submit", "disabled", "Generating a disabled submit button") %>
<%= guide_example("submit", "multiple", "A group of buttons") %>

<p><%= link_to("Back to guide", root_path) %></p>
